---
description: "Article III (Verification) of the Constitution. Not an interrupt: the constitution is one resident file, already in context."
---

# Article III — Verification

The constitution is one file — `CONSTITUTION.md` — already in your context. There is nothing to retrieve and no signal this rule file could add: regex cannot see oracle independence, observer fit, or sandwich order — the defects this article names — so no condition is declared here.

Apply Article III where it governs: when you author a test or judge one, the article's rules (the trophy's investment order, properties where the published surface cannot reach, mutation as the measure, the oracle is never the system under test, published contracts pinned before a path is deleted, gate keys recomputed) are in context and binding.
